'use client'

import React from 'react';
import { motion } from "framer-motion";

const CircleAnimate = (props) => {
    return (
        <>
            <motion.dev {...props} />
        </>
    );
};

export default CircleAnimate;